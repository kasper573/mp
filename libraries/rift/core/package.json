{
  "name": "@rift/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --typecheck",
    "bench": "tsx scripts/benchmark.ts",
    "profile": "node --expose-gc --import tsx scripts/profile.ts"
  },
  "dependencies": {
    "@mp/std": "workspace:*",
    "@preact/signals-core": "catalog:",
    "@rift/event": "workspace:*",
    "@rift/types": "workspace:*"
  },
  "devDependencies": {
    "mitata": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
