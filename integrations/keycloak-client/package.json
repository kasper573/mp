{
  "name": "@mp/keycloak-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc && tsx esbuild.ts",
    "provision": "NODE_TLS_REJECT_UNAUTHORIZED=0 tsx src/provision.ts",
    "devenv": "dotenvx run -f ../../docker/.env.dev ../../docker/.env.shared -- pnpm"
  },
  "dependencies": {
    "@keycloak/keycloak-admin-client": "26.1.2",
    "@mp/game": "workspace:*",
    "@mp/logger": "workspace:*",
    "@mp/std": "workspace:*"
  },
  "devDependencies": {
    "@mp/tsconfig": "workspace:*",
    "@mp/build": "workspace:*",
    "@types/node": "catalog:"
  }
}
